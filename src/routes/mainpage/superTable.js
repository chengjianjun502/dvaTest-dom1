import React, { Component } from 'react';
import SuperTable from '../../components/MainPage/SuperTable';

export default class superTable extends Component {
    render() {
        return (
            <div>
                <SuperTable />
            </div>
        )
    }
}
